# 🚀 Quick Reference Card

## Launch Web App (3 Steps)

```bash
# 1. Train model (if needed)
source venv/bin/activate && python quick_demo_train.py

# 2. Launch web server
./run_web.sh

# 3. Open browser
# Go to: http://localhost:5001
```

**Launch now:** `./run_web.sh` → Open `http://localhost:5001` 🚀
